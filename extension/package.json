{
  "name": "scm-diff-stats",
  "displayName": "SCM Diff Stats",
  "description": "Per-file +/- line stats for working tree changes and recent commits, per repo/worktree.",
  "version": "0.6.0",
  "publisher": "simon",
  "engines": { "vscode": "^1.75.0" },
  "categories": ["Other"],
  "main": "./extension.js",
  "activationEvents": ["onStartupFinished"],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        { "id": "diffStats", "title": "Diff Stats", "icon": "media/diff-stats.svg" }
      ]
    },
    "views": {
      "diffStats": [
        { "type": "webview", "id": "scmDiffStats", "name": "Diff Stats" }
      ]
    },
    "commands": [
      { "command": "scmDiffStats.refresh", "title": "Refresh Diff Stats", "icon": "$(refresh)" }
    ],
    "menus": {
      "view/title": [
        { "command": "scmDiffStats.refresh", "when": "view == scmDiffStats", "group": "navigation" }
      ]
    },
    "configuration": {
      "title": "SCM Diff Stats",
      "properties": {
        "scmDiffStats.syncTestCommand": {
          "type": "string",
          "default": "",
          "markdownDescription": "Shell command for the per-repo **⇣ sync + test** button (empty hides the button). Placeholders: `${name}` (worktree folder name), `${branch}`, `${repoPath}`. Runs via `bash -lc` in the repo. Non-zero exit launches a Claude fix agent in a terminal at the worktree."
        }
      }
    }
  }
}
